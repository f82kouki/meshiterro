<div>
  <h3><%=@user.name%></h3>
  <%=image_tag @user.get_profile_image(100,100)%>
  <% if @user == current_user %>
  <p><%=link_to "プロフィール編集する",edit_user_path(@user.id) %></p>
  <% end %>
</div>

<%= render 'post_images/lists', post_images: @post_images %>
